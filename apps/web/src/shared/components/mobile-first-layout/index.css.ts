import { style } from "@vanilla-extract/css";

export const container = style({
  minHeight: "100dvh",
  height: "100%",
  maxWidth: "600px",
  margin: "0 auto",
});

export const contentWrapper = style({
  width: "100%",
});
