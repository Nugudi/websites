/**
 * Cafeteria UI Features Types
 *
 * IMPORTANT:
 * - These are UI-specific custom types, not OpenAPI types
 * - Used for form state, local transformations, and UI-specific logic
 * - For API-related types, use types/cafeteria.type.ts or types/cafeteria-review.type.ts
 */

export * from "./cafeteria-review-comment";
export * from "./request-register";
